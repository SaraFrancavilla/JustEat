# Remember to make a .env.local in agentA and agentB folders (locally)
The file should say LLM_API_KEY=<llm_token>

# Stuff that still doesn't work

- Parcel handoff missions between agents (A picks up and gives it to B who delivers) / coordination with other agent
- Missions with exact/min/max numbers of parcels to pickup/deliver
- Comparing a version that actually uses PDDL to this one to show which is faster (questa ora non usa praticamente mai il PDDL ma è molto veloce e pensavo che almeno fare questo confronto sarebbe una buona cosa per il report)
- Reorganizing mission logic overall
